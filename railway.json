{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "gunicorn mycareportal.wsgi:application --bind 0.0.0.0:${PORT:-8000} --timeout 120"
  }
}